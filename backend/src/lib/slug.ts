/** Lowercase, hyphenate, strip non-alphanumerics. Used for brand slugs + group keys. */
export function slugify(input: string): string {
  return input
    .toLowerCase()
    .trim()
    .replace(/[^a-z0-9]+/g, '-')
    .replace(/^-+|-+$/g, '')
}
